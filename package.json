{
  "name": "harmoni",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "cd server && node app.js",
    "mobile": "cd mobile && npx expo start"
  },
  "dependencies": {
    "react-native-worklets": "^0.10.1"
  }
}
